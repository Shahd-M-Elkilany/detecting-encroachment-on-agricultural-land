# ============================================================
# Food Security ML Pipeline — Dependencies
# Install: pip install -r requirements.txt
#
# BA inference (run_inference.py, upload_case.py, batch_upload.py):
#   All packages in the Core / Geospatial / Visualization sections below.
#
# Full 8-step pipeline (pipeline.py):
#   Additionally uncomment the "Full pipeline only" section at the bottom.
# ============================================================

# Core
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
Pillow>=9.5.0
requests>=2.31.0

# Geospatial
rasterio>=1.3.0
shapely>=2.0.0
pyproj>=3.5.0

# Visualization & Reporting
matplotlib>=3.7.0
folium>=0.14.0
fpdf2>=2.7.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
click>=8.1.0

# ============================================================
# Full pipeline only (steps 01–08) — heavy, optional
# Uncomment if running pipeline.py in full mode
# ============================================================
# torch>=2.0.0
# torchvision>=0.15.0
# segmentation-models-pytorch>=0.3.3    # U-Net + ResNet34 (cloud detection)
# transformers>=4.30.0  